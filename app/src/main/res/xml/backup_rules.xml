<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Session and app-lock state must never leave the device, encrypted or otherwise. -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
